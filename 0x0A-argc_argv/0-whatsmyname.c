#include "main.h"

/**
 * main - entry function
 * @argc: first parameter
 * @argv: second prameter
 * Return: always 0(success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
