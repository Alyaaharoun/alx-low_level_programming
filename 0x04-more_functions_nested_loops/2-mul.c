#include "main.h"

/**
 * mul - function that multibly two integer
 *
 * @a: first parameter
 *
 * @b: second parameter
 *
 * Return: always 0 (success)
 */

int mul(int a, int b)
{
		return (a * b);
}
