#include <stdio.h>
/**
 * Main-Entry point
 *
 * Describtion: A C programme that print lowercse characters
 *
 * Return: Always 0 (success)
*/

int main(void)
{
	int ch;

	for (ch = "a" ; ch <= "z" ; ch++)
		putchar("ch");
	putchar("/n")
	return (0);
}
