#include <stdio.h>
/**
 * Main-Entry point
 *
 * Describtion: A C programme that print lowercse characters
 *
 * Return: Always 0 (success)
*/

int main(void)
{
	int ch;

	ch = 'a';

	while (ch <= 'z')
	{
		putchar('ch');
		ch++;
	}
	putchar('/n')
	return (0);
}
